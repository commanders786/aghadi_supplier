platform :ios, '15.0'  # Set to a version supported by your Xcode (e.g., 15.0 for Xcode 16)
use_frameworks!

target 'YourAppTarget' do
  # Add dependencies here, if any, e.g.:
  # pod 'Alamofire', '~> 5.6'
  # pod 'Firebase/Analytics'
end